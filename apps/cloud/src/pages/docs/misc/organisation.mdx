import { Layout } from '@components/docs/layout';

export const meta = {
  title: "Organisation Usage"
}

## Organisation Usage

We simply request Organisations donate to help keep this project going 😊

export default ({ children }) => <Layout meta={meta}>{children}</Layout>