import { Layout } from '@components/docs/layout';

export const meta = {
  title: "Changelog"
}

## Changelog

Changelog for all supported components and future plans

### Cloud

- 1.0.0 - (TBA)
  - Initial Release

- Planned features
  - Email only login
  - Search bar for projects page
  - Improve user settings page UI/UX
  - Audit log for config changes
  - Project Icons

### Node CLI

- 1.0.0 - (TBA)
  - Initial Release

- Planned features
  - Merge previously downloaded .env file with newly downloaded file
  - Insert access token into downloaded env file
  - Login via Website

export default ({ children }) => <Layout meta={meta}>{children}</Layout>